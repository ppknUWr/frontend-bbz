import { makeStyles, createStyles } from "@material-ui/core";

export const databaseTitleTypoStyles = makeStyles(
  createStyles({
    root: {
      color: "black",
      fontSize: "2.5vh",
    },
  })
);
