import { makeStyles, createStyles } from "@material-ui/core";

export const sidebarIconButtonStyles = makeStyles(
  createStyles({
    root: {
      borderRadius: 4,
      marginRight: 40,
      display: "none",
    },
  })
);